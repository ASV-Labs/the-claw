# Contributing

Keep the machine: one sentence in, every prize considered, matches lift. Place pits (Eats, Pubs, Do) ask for a city before they fill.

```bash
npm install
npm test
npm run build
```

Do not commit `.env.local`.
